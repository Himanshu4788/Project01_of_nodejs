<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body style="color: aliceblue;" >
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form ">
            <form action="/create" method="post">
                <div class="title_and_task">
                    <input class="block1 w-full  rounded-md  px-5 py-2 bg-zinc-800" name="title" type="text" placeholder="title goes here" >
                    <textarea class= "block2 w-full rounded-md mt-2  px-5 py-2 bg-zinc-800  " name="details" placeholder="write your task details here"></textarea>
                </div>
                <div class="create_task">
                    <input class="block3 mt-2" type="submit" value="create tasks">
                </div>
            </form>
        </div>

        <div class="tasks  ">
            <% if (files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task">
                        <h1> <%=val %> </h1>
                        <div class="extra">
                            <a href="/files/<%= val %>" style="color: rgb(36, 36, 233); font-size: 12px;">Read More.</a>
                            <a class="edit_file_option" href="/edit/<%= val %>" >Edit Filename</a>
                        </div>
                    </div>
                <% }); %>       
            <% } else { %> 
                <h3 class="text-zinc-600">No task yet</h3>
            <% } %>
            
         </div>
    </div>
    <script src="/javascripts/script.js"></script>
</body>
</html>